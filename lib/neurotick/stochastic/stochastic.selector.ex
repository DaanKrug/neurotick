defmodule Neurotick.Stochastic.Selector do

  alias Krug.NumberUtil
  alias Neurotick.Probability.Selector
  
  def select_neuron(_neurons) do
    probability = 0.1
    #Selector.select(neurons,probability)
  end
  
  
end